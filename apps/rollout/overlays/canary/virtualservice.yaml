apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rollouts-demo-vsvc
spec:
  gateways:
  - canary
  hosts:
  - canary
  http:
  - name: primary
    route:
    - destination:
        host: canary-stable
        port:
          number: 8080
      weight: 100
    - destination:
        host: canary
        port:
          number: 8080
      weight: 0
  tls:
  - match:
    - port: 8080
      sniHosts:
      - rollouts-demo-vsvc.local
    route:
    - destination:
        host: canary-stable
      weight: 100
    - destination:
        host: canary-canary
      weight: 0
